{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "legacy-node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/server.js"
        }
    ]
}